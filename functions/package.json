{
  "name": "mermaid-flow-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:notionProxy,functions:aiGenerate",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-functions": "^5.1.1"
  }
}
